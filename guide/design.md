# Guide Design

The guide section should cover three personas with increasing depth:

- **Consumers** get the simplest path: connect, navigate, search, read
- **Analysts** get exploration patterns: depth queries, references, graph visualization, history
- **Builders** get the full picture: file structure, compilation, pipelines, validation, design.md conventions

Each guide should be self-contained and reference specific MCP tools / GraphQL queries.
