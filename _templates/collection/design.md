# {{name}} Collection Design

## What a well-formed child looks like
- Must have `name` and `description` in frontmatter
- Should link to related Things in other collections

## Conventions
- Each child Thing gets its own directory with `index.md`
- Use `design.md` to capture domain-specific guidance
